build:
  config:
    RUBY_VERSION: 2.7.5
    PG_CLI_VERSION: 13+225
    BUNDLER_VERSION: 2.2.31
    BUNDLE_GEMFILE: Gemfile
  docker:
    web: docker/dev/Dockerfile
run:
  web: bundle exec rails server
release:
  image: web
  command:
    - bundle exec rails db:migrate
